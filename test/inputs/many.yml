jobs:
  build:
    docker:
      - image: circleci/node:10
    working_directory: ~/repo
    steps:
      - banners/print:
          message: "Starting build"
      - banners/print:
          message: "Starting another"
      - banners/print:
          message: "Starting more"